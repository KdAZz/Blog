server:
  port: 8000

spring:
  application:
    name: service-article
  cloud:
    nacos:
      # 注册中心
      discovery:
        server-addr: http://192.168.1.107:8848
      # 配置中心
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml
        shared-configs[0]:
          data-id: blog-common.yaml
          refresh: true
logging:
  level:
    spring.: DEBUG